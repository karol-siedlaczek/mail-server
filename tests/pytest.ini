[pytest]
python_files = test_*.py itest_*.py
markers =
    integration: test requires the live compose stack (run via `make itest`)
    optional: test may pass or be skipped depending on stack configuration

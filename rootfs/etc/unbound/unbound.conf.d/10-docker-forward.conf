# Rendered at boot by render-config: one forward-zone per configured backend
# hostname (PG_HOST / REDIS_HOST / CLAMAV_HOST / RELAYHOST) pointing at Docker's
# embedded DNS, so service discovery keeps working while everything else is
# resolved recursively. This shipped copy is an empty placeholder.
